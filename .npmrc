# Allow safe build scripts for image processing and resolver packages
enable-pre-post-scripts=true
ignored-build-dependencies=

# Allow specific packages that are safe to run build scripts
# sharp - image processing library (safe)
# unrs-resolver - resolver utility (safe)
